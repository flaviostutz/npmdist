# AGENTS.md

Always run commands `make build`, `make lint`, `make test` to check changes, in this order
Run `make lint-fix` before trying to fix any linting issues
Use pnpm, pnpm dlx, pnpm exec
Never use emojis anywhere
