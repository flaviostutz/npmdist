# README.md

This is an example project that publishes docs, configs and datasets as a package.

## Getting started

In order to prepare this package to be published to a NPM registry, do the following steps:

```sh
# changes package.json for required configurations
pnpm dlx folder-publisher init --folders configs,data,docs

```
